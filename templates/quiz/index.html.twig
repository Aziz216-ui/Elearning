<html>
<head>
    <style>
        body { text-align: center; font-family: Arial; }
        h1 { margin-top: 100px; }
        .box { border: 3px solid #000; padding: 50px; }
    </style>
</head>
<body>

<div class="box">
    <h1>Certificat de réussite</h1>

    <h2>{{ user.name }} {{ user.lastname }}</h2>

    <p>A réussi le cours :</p>
    <h3>{{ cours.title }}</h3>

    <p>Score : <strong>{{ score|number_format(0) }} / 100</strong></p>

    <p>Date : {{ "now"|date('d-m-Y') }}</p>
</div>

</body>
</html>

