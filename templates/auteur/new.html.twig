{% extends 'base1.html.twig' %}

{% block title %}New Auteur{% endblock %}

{% block body %}
    <div class="max-w-2xl mx-auto px-4 py-8">
        <div class="w-full bg-white dark:bg-slate-850 shadow-xl dark:shadow-dark-xl rounded-2xl p-6">
            <h1 class="text-2xl font-bold text-slate-800 dark:text-white mb-6">Nouvel Auteur</h1>

            {{ include('auteur/_form.html.twig') }}

            <div class="flex justify-end gap-3 pt-4">
                <a href="{{ path('app_auteur_index') }}" class="px-4 py-2 text-sm font-semibold rounded-lg border border-slate-300 dark:border-slate-700 text-slate-700 dark:text-slate-300 hover:bg-slate-50 dark:hover:bg-slate-800">Annuler</a>
            </div>
        </div>
    </div>
{% endblock %}
