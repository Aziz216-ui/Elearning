{{ form_start(form, { attr: { novalidate: 'novalidate' } }) }}
    {{ form_row(form.user) }}
    {{ form_row(form.plan) }}
    {{ form_row(form.status) }}
    {{ form_row(form.startDate) }}
    {{ form_row(form.endDate) }}
    {{ form_row(form.autoRenew) }}
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
