{% extends 'base1.html.twig' %}

{% block title %}New Cours{% endblock %}

{% block body %}
    <div class="w-full px-4 lg:px-0 py-8">
        <div class="max-w-3xl mx-auto">
            <h1 class="text-2xl font-bold text-slate-800 dark:text-white mb-6 text-center"> Ajouter Cours</h1>
            <div class="w-full bg-white dark:bg-slate-850 shadow-xl dark:shadow-dark-xl rounded-2xl p-6">

                {{ include('cours/_form.html.twig', {
                    button_label: 'Ajouter',
                    button_class: 'px-5 py-2.5 text-sm font-semibold text-white rounded-lg bg-black hover:bg-slate-900 shadow-md hover:shadow-lg',
                    background_right: true
                }) }}

                <div class="flex justify-center gap-3 pt-4">
                    <a href="{{ path('app_cours_index') }}" class="px-4 py-2 text-sm font-semibold rounded-lg border border-slate-300 dark:border-slate-700 text-slate-700 dark:text-slate-300 hover:bg-slate-50 dark:hover:bg-slate-800">Annuler</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

