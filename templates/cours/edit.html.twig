{% extends 'base1.html.twig' %}

{% block title %}Edit Cours{% endblock %}

{% block body %}
    <div class="w-full px-4 lg:px-0 py-8">
        <div class="max-w-3xl mx-auto w-full bg-white dark:bg-slate-850 shadow-xl dark:shadow-dark-xl rounded-2xl p-6">
            <h1 class="text-2xl font-bold text-slate-800 dark:text-white mb-6">Modifier le Cours</h1>

            {{ include('cours/_form.html.twig', {
                'form': form,
                'button_label': 'Modifier',
                'button_class': 'px-5 py-2.5 text-sm font-semibold text-white rounded-lg bg-black hover:bg-slate-900 shadow-md hover:shadow-lg',
                'background_right': true
            }) }}

            <div class="flex justify-between items-center gap-3 pt-4">
                <a href="{{ path('app_cours_index') }}" class="px-4 py-2 text-sm font-semibold rounded-lg border border-slate-300 dark:border-slate-700 text-slate-700 dark:text-slate-300 hover:bg-slate-50 dark:hover:bg-slate-800">Retour</a>
                <div class="flex items-center gap-3">
                    {{ include('cours/_delete_form.html.twig', {'cour': cour}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
